#include<stdio.h>
#include<stdlib.h>

typedef struct node
{
	int data;
	struct node *left;
	struct node *right;
}node;

main()
{
	int n,i;
	struct node *parent =(*node)malloc(sizeof(node));
	parent->data=10;
	parent->left=NULL;
	parent->right=NULL;
	scanf("%d",&n);
	for(i=0;i<n;i++)
	{
		scanf(%d,&input);
		if(input>parent->data)
		{
			parent->data=
		}
	}
	
}
